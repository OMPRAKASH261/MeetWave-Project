## Technologies 
Data - MongoDb

# Backend - technologies
1. Bcrypt
2. Crypto
3. Socket
4. Express js

# Frontend - technologies
1. Material UI
2. WebRTC
3. React
4. Axios
5. CSS

## Backend - dependencies
cd backend
npm init
`npm install express mongoose nodemon bcrypt crypto dotenv http-status socket.io  `
`npm i cors --save`
`npm run dev`

### use thunder client to post data and check (register and login) in mongodb

## frontend
`npx create-react-app frontend`
`cd frontend`
`npm start`

react router -> `npm i react-router-dom`

## material ui 
fontend terminal - `npm install @mui/material @emotion/react @emotion/styled`
`npm install axios http-status socket.io-client`
`npm install @mui/icons-material @mui/material @emotion/styled @emotion/react`